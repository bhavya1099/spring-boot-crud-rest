# ********RoostGPT********

# Test generated by RoostGPT for test test-api-test-report using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=11e354dac0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_others.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Successful API Responses Test"
    flow:
      - log: "Testing missing request body case for '/availabilityCheck'. Expect failure response."
      - post:
          url: "/availabilityCheck"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 400
            contentType: "application/json"
            hasProperty: "error"
      - think: 2

      - log: "Testing invalid payload type for '/availabilityCheck'. Expect failure response."
      - post:
          url: "/availabilityCheck"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            type: "string"    # Example of invalid type if API expected an object
          expect:
            statusCode: 400
            contentType: "application/json"
            hasProperty:
              - "error"
              - "message"
      - think: 2

      - log: "Testing unauthorized request for '/availabilityCheck'. No Authorization header."
      - post:
          url: "/availabilityCheck"
          headers: {}
          expect:
            statusCode: 401
            contentType: "application/json"
            hasProperty: "error"
      - think: 2

      - log: "Testing invalid URL for '/availabilityCheck' endpoint. Expect failure response."
      - post:
          url: "/availabilityCheckINVALID"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            statusCode: 404
            contentType: "application/json"
            hasProperty: "message"
      - think: 2
