# ********RoostGPT********

# Test generated by RoostGPT for test test-api-test-report using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=11e354dac0
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_success.csv"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Availability Check API Success Response Validation"
    flow:
      - log:
          message: "Starting POST request to validate availabilityCheck API with valid payload."
          
      - post:
          url: "/availabilityCheck"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            dynamicValue: "{{ dynamicValue }}"
          
      - expect:
          statusCode: 200
      
      - expect:
          hasProperty:
            - "control"
            - "control.type"
            - "control.message"
            - "control.code"
            - "resource"
            - "resource.resource"
            - "resource.resource.inventoryId"
            - "resource.resource.maxBandwidth"
            - "resource.resource.availabilityType"
            - "resource.resource.availabilityCode"
            - "resource.resource.availabilityDescription"
            - "resource.resource.products"

      - think:
          duration: 2
